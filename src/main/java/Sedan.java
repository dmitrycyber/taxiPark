public class Sedan extends Car {

    public Sedan(String model, String make, double fuelConsumption, int countOfSeatingPositions, String type) {
        super(model, make, fuelConsumption, countOfSeatingPositions, type);
    }
}

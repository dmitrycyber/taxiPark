public class Minivan extends Car {

    public Minivan(String model, String make, double fuelConsumption, int countOfSeatingPositions, String type) {
        super(model, make, fuelConsumption, countOfSeatingPositions, type);
    }
}
